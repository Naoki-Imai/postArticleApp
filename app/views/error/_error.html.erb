<% if @menu.errors.any? %>
  <div class="alert alert-warning">
    <ul>
      <% @menu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>